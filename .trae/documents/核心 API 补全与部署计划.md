根据您的要求，我将优先解决阻碍前端运行的 **API 缺失问题**，并推进 **代码推送与验证**。由于监控、Qdrant 缓存优化和 CI/CD 改进属于“第二阶段（性能与运维优化）”的长期任务，本轮操作将集中在确保核心功能闭环上。

以下是针对您 **Step 1 & 2** 需求的执行计划：

### **阶段一：核心 API 补全 (Backend Implementation)**
*   **目标**: 解决前端因后端接口缺失而无法展示任务列表的问题。
*   **动作**:
    1.  **实现 `GET /reports/stats/tasks`**:
        *   修改 [reports.py](file:///d:/SalesBoost/app/api/endpoints/reports.py)，添加任务聚合统计接口。
        *   逻辑：基于 `Session` 表统计不同状态（未开始、进行中、已完成）的会话数量。
    2.  **实现 `GET /reports/tasks`**:
        *   修改 [reports.py](file:///d:/SalesBoost/app/api/endpoints/reports.py)，添加任务列表接口。
        *   逻辑：返回带有课程信息、标签和进度的任务列表数据（初期可基于场景配置生成，或查询 Session 历史）。
    3.  **添加后端测试**:
        *   创建 `tests/test_api_reports_tasks.py`，验证上述两个接口的返回结构和状态码。

### **阶段二：部署与验证 (Deployment & Verification)**
*   **目标**: 将代码推送到生产环境，并验证全流程。
*   **动作**:
    1.  **代码提交**: 将包含 API 修复和配置更新的所有代码推送到 GitHub `main` 分支。
    2.  **触发部署**: 确认 Vercel (前端) 和 Render (后端) 的自动构建被触发。
    3.  **生产环境验证**: 指导您进行线上测试（登录 -> 练习 -> 报告）。

---

**关于性能与运维优化（第二阶段）的说明**:
您提到的 Prometheus 监控、Qdrant 缓存和 E2E 测试是非常专业的优化项，但为了保证当前 MVP 版本的快速上线，建议将这些任务列入 **v1.2 版本规划**（即在本次核心功能跑通后立即启动）。本轮我将专注于让系统“先跑起来且跑对”。

**是否同意我优先执行 API 补全和代码推送任务？**
