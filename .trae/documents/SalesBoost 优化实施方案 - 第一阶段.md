您提出的“七步优化方案”非常系统且专业，是推动 SalesBoost 从“MVP 原型”走向“成熟产品”的完美路线图。

鉴于当前项目处于 **后端核心成熟、前端空壳化、文档齐全但需更新** 的状态，我建议我们将这七步拆解为三个阶段执行。

本轮任务我将聚焦于 **“第一阶段：基础夯实与核心打通”**，重点解决您方案中 **Step 2 (技术优化)** 和 **Step 3 (功能完善)** 的燃眉之急，即彻底解决前端 Mock 数据问题，让系统“活”起来。

### **第一阶段执行计划：核心打通与质量基线 (本轮执行)**

#### **1. 技术优化 (Step 2)**
*   **前端 API 真实化 (Critical)**:
    *   目前前端 `api.ts` 是 Mock 的，这是最大的技术债务。
    *   **行动**: 封装通用的 `HttpClient` (带拦截器)，并重写 `TaskService`, `PersonaService`, `HistoryService`，对接后端 `/api/v1/reports`, `/api/v1/sessions` 等真实接口。
*   **增加前端测试 (Major)**:
    *   目前前端测试覆盖率 < 10%。
    *   **行动**: 为新编写的 API Service 添加单元测试，并为 `Dashboard` 页面添加集成测试。

#### **2. 功能完善 (Step 3)**
*   **数据大屏对接**:
    *   **行动**: 修改 `DashboardPage.tsx`，使其真实展示后端计算的“能力雷达图”和“训练进度”，替换掉静态的假数据。
*   **用户体验闭环**:
    *   **行动**: 在 `PracticeRoom` 结束后，自动跳转到 `History` 详情页查看刚刚生成的评估报告（目前流程是断的）。

#### **3. 文档体系建设 (Step 4)**
*   **行动**: 创建 `PROJECT_ROADMAP.md`，将您提出的 7 步优化方案固化为项目文档，作为后续开发的纲领。

---

### **后续阶段规划 (未来执行)**

*   **第二阶段：性能与运维 (Step 4, 5)**
    *   实施 Prometheus + Grafana 监控。
    *   优化 Qdrant 向量检索速度 (Rerank 缓存)。
    *   完善 CI/CD，增加自动化 E2E 测试。
*   **第三阶段：团队与长期规划 (Step 6, 7)**
    *   建立代码审查 (Code Review) 规范。
    *   制定 Micro-services 拆分计划 (如果用户量激增)。

---

**待确认**: 是否同意我立即开始执行 **第一阶段** 的任务，优先打通前后端数据链路？
