class SimpleAuditGate:
    """
    MVP ?? Gate?
    - ?????????????????
    """
    def check(self, text: str, meta: dict) -> bool:
        banned = ["????", "100%??"]
        return not any(x in text for x in banned)
