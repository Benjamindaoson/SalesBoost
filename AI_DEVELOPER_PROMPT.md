# 帮我设计并实现一套硬核的工业级AI系统：实时销售辅助引擎 (Real-time Sales Copilot Engine)

## 1. 项目定位与核心原则
这不仅是一个普通的客服对话机器人，而是一个面向高客单价、高压销售实战场景的**决策外脑（Copilot）**。
- **极限低延迟**：端到端响应必须控制在 1.5 秒以内，大量采用流式与并行调用的设计。
- **先思考后开口**：废弃直接生成回复的做法，强制引入“推理链（Reasoning Chain）”推演用户隐性意图。
- **数据驱动与自演进**：将系统的重心从“调 Prompt”转移到“收集业务转化数据”，为强化学习（DPO/RL）和自动话术优化（APO）做好底层基建。

## 2. 技术栈选取
- **主要语言及框架**：Python 3.11+、FastAPI（原生支持高并发与流式响应）
- **核心编排框架**：LangGraph（负责微观 Agent 节点流转） + 原生 Python FSM 状态机（负责宏观销售阶段跃迁控制，确保极度可控）
- **关系型与向量数据库**：PostgreSQL + pgvector（核心业务数据、Prompt 版本中心、带有时间半衰期的适应性记忆）
- **缓存与并发限流**：Redis（分布式并发信号量控制、Working Memory 高频读写）
- **模型路由与观测**：自研 Model Gateway 模型网关层、OpenTelemetry 链路追踪

## 3. 核心功能及模块开发要求：

### 1）宏观 FSM + 微观 LangGraph 双引擎总控
- **要求**：坚决摒弃用 LLM 来做全局路由判断。由一套定义清晰的 FSM 类来管理销售漏斗阶段（破冰 -> 需求诊断 -> 价值提案 -> 异议处理 -> 关单）。
- **实现**：LangGraph 作为 FSM 内一个个独立阶段的具体执行 DAG（有向无环图），确保“大局确定，细节智能”。

### 2）销售推理链（Sales Reasoning Chain）节点
- **要求**：在生成回复前，先经过 Reasoning 大模型节点。
- **实现**：该节点仅输出结构化 JSON，解析客户的“表面字意（Literal）”与“隐性忌惮（Hidden）”，并结合当前 FSM 阶段，制定下一句话的**战术基调（Tactics）**。将此结论传递给后续的轻量级生成大模型。

### 3）流式投机护栏引擎（Streaming Speculative Guard）
- **要求**：严禁采用传统的“先生成完整句子、查验无误后再输出”的阻塞式做法。
- **实现**：主 LLM 以 Server-Sent Events (SSE) 流式返回文本，同时旁路拉起一个轻量级安全校验。一旦捕获到虚假承诺或合规雷区，在句子/Token级别立刻执行截断（Truncate）和无缝重写（Rewrite）。

### 4）时间衰减过滤的记忆中枢（Adaptive Memory with Importance Decay）
- **要求**：抛弃将所有对话历史一股脑塞进上下文的初级做法。
- **实现**：落库到 PostgreSQL 的每一个关键用户实体（Slot/Profile），都必须包含 `timestamp` 和 `importance_score` 字段。每次上下文提取时，执行时间半衰期公式，自动遗忘低价值过期信息。

### 5）面向 DPO/RL 的数据飞轮与评估钩子（APO & Data Flywheel）
- **要求**：业务代码中完全消除硬编码（Hardcode）的系统提示词，所有话术策略从数据库或配置中心加载。
- **实现**：每次完整对话或 FSM 阶段跃迁时，必须强制触发**埋点落库（Data Logging）**，保存用户最后实际转化结果的 Preference Pair（Chosen vs Rejected 模型回复），暴露标准接口用于外部 Reward Model 打分和离线训练。

## 4. 实施要求与步骤安排：
基于上述的架构理念和技术栈清单，请在此目录下，为我搭建整个项目架构。
1、先建立符合工业级大型工程规范的目录结构骨架。
2、设计 FSM 实体类及 LangGraph 核心图状结构的伪代码或基类。
3、逐步将 `Reasoning Chain`、`Streaming Guard` 等高阶能力抽象封装后填充进去。
请保持代码极高的抽象度、重用性和最佳实践规范，不要出现冗余的意大利面条式代码。开始执行吧！
